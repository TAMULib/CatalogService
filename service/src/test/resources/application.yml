server:
  port: 9000
  servlet:
    context-path:

security.basic.enabled: false

spring.profiles.active: test

# (is overwritten by logback-test.xml)
logging:
  file:
    name: logs/catalog-service-test.log
  level:
    com.zaxxer: ERROR
    edu.tamu: WARN
    org:
      hibernate: OFF
      springframework: ERROR

app:
  http.timeout: 10000
  security.allow-access: http://localhost,http://localhost:8080
  whitelist: 127.0.0.1
